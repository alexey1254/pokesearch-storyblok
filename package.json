{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "dev-https": "local-ssl-proxy --source 3010 --target 3000 --cert localhost/localhost.pem --key localhost/localhost-key.pem & nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "nuxt": "^3.1.0"
  },
  "dependencies": {
    "@storyblok/nuxt": "^5.0.2"
  }
}
